# class MySum(object):
#     def __init__(self, y, x):
#         self.y = y
#         self.x = x
def my_sum(x, y):
    '''
    takes two numbers and return (x and y) and returns its sum
    '''
    if type(x) or type(y) == str:
        return "Invalid input"
    elif ty(x) or type(y) == list:
    	return "invalid input"
    else:
        return x + y
    

    

